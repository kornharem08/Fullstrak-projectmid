<% include ../partials/header %>

    <!-- <h1><%= fullname %></h1>
<% hobbies.forEach(function(hobbies) { %>
        <li><%= hobbies %></li>
    <% }); %>
<h3><%= bdate %></h3> -->
<div class="jumbotron jumbotron-fluid">
        <div class="container">
            <h1 class="display-3 text-center"><b><%= fullname %></b></h1>
            <h2 class="text-center">5930213034 software engineering</h2>
            <h3 class="display-3 text-center">Free-Time Activities</h3>
            <hr class="my-2 text-center">
            <% hobbies.forEach(function(hobbies) { %>
                <li><%= hobbies %></li>
            <% }); %>
        <h3 class="text-center"><%= bdate %></h3>
        </div>
    </div>
 <% include ../partials/footer %>